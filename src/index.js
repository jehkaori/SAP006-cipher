/*import cipher from './cipher.js';

console.log(cipher);

function encriptar(elemento){
    let mensagem = document.getElementById('textoriginal')
    let desloc = document.getElementsByName('Deslocamento')
    let mensagemCifrada = ((codigoDaLatraASC - codigoLetra + desloc) % tamDoAlfabeto)+ codigoLetra
    
  }
  
  function desencriptar (elemento){
}
   let string = document.getElementById("txtoriginal")
  let offset = document.getElementById("desloc")

  export default cipher

  /*
  decode: function decode(string, offset){
    offset = parseInt(offset, 10)
    let result = "", message
    //((codigoDaLatraASC - codigoLetra + desloc) % tamDoAlfabeto) + codigoLetra
  
      for (let i=0; i<string.length; i++){
        if(string.charCodeAt(i)>=90 && string.charCodeAt(i)<=65){
        message = ((string.charCodeAt(i)-65 - desloc)%26)+65
        }
        else if (string.charCodeAt(i)>=122 && string.charCodeAt(i)<=97){
        message = ((string.charCodeAt(i)-97 - desloc)%26)+122
        }
        else if (string.charCodeAt(i)===32){
          message = 32
        }
        result+=String.fromCharCode(message)
      }
      return result
    }
  
} 
export default cipher

  */

  